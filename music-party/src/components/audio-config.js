/**
 * 音频处理的中央配置文件
 * 所有需要调整的参数都集中在此处。
 */

// =================================================================
// 自动回放增益算法配置
// =================================================================
export const AUTOGAIN_CONFIG = {
  // 最终的目标响度 (LUFS)
  TARGET_LOUDNESS_LUFS: -18.0,

  // 切歌时，音量控制器重置到的初始增益值 (dB)
  INITIAL_GAIN_DB: -3.0,
  
  // 【新增】自动增益激活延迟（秒），在此时间后，系统才开始自动调节音量
  AUTOGAIN_ACTIVATION_DELAY_SECONDS: 2.0,

  // --- 门限算法参数 ---
  // 绝对静音门限 (LUFS)，低于此值的瞬时响度将被直接忽略
  ABSOLUTE_GATE_LUFS: -40.0,

  // 相对门限的动态范围 (LU)，相对门限 = 平均响度 - 此值
  RELATIVE_GATE_RANGE_LU: 12.0,

  // 相对门限的启用延迟时间 (秒)，在此时间后，算法开始使用相对门限
  RELATIVE_GATE_DELAY_SECONDS: 3.0,
};


// =================================================================
// PID 控制器参数
// =================================================================
export const PID_CONFIG = {
  mass: 10,
  kp: 2,
  ki: 0.0001,
  kd: 10,
  maxSpeedDbPerSecond: 4.0,
};


// =================================================================
// “砖墙”限幅器参数
// =================================================================
export const LIMITER_CONFIG = {
  threshold: -3.0,
  knee: 3,
  ratio: 10,
  attack: 0.001,
  release: 0.1,
};
